apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ include "workbench-operator.fullname" . }}-serving-cert
  labels:
  {{- include "workbench-operator.labels" . | nindent 4 }}
spec:
  dnsNames:
  - '{{ include "workbench-operator.fullname" . }}-webhook-service.{{ .Release.Namespace
    }}.svc'
  - '{{ include "workbench-operator.fullname" . }}-webhook-service.{{ .Release.Namespace
    }}.svc.{{ .Values.kubernetesClusterDomain }}'
  issuerRef:
    kind: Issuer
    name: '{{ include "workbench-operator.fullname" . }}-selfsigned-issuer'
  secretName: webhook-server-cert